# Displays database queries in the console
logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# Database URL
spring.datasource.url=jdbc:hsqldb:mem:api_rest_users_test;DB_CLOSE_DELAY=-1
# Database User
spring.datasource.username=amtineztest
# Database Password
spring.datasource.password=apirestuserstest
# Configure hibernate to create database when tests start but delete when finished
spring.jpa.hibernate.ddl-auto=create-drop
# Mail Server
spring.mail.host=localhost
spring.mail.port=1026
spring.mail.protocol=smtp
