# Stop Lombok from looking at parent directories for more configuration files
config.stopBubbling = true
# Prevent Lombok generated code from influencing coverage
lombok.addLombokGeneratedAnnotation = true